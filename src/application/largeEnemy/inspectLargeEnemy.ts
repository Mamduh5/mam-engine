import { auditChangedFiles, captureWorkspaceState } from "../../infrastructure/files/changedFileAudit";
import { ErrorCodes } from "../../shared/errorCodes";
import { operationResult, type OperationResult } from "../../shared/operationResult";
import { isLoadedLargeEnemyBundle, loadValidLargeEnemyBundle } from "./largeEnemyOperationSupport";

export async function inspectLargeEnemy(workspaceRoot: string, inputFile: string): Promise<OperationResult> { const command = "large-enemy.inspect"; const input = { file: inputFile }; const before = await captureWorkspaceState(workspaceRoot); const loaded = await loadValidLargeEnemyBundle(workspaceRoot, inputFile); const audit = auditChangedFiles(before, await captureWorkspaceState(workspaceRoot), []); if (!audit.ok) return operationResult({ command, status: "failed", input, errors: [{ code: ErrorCodes.LargeEnemyWriteBlocked, message: "Read-only large-enemy inspection changed unexpected files", details: { unexpectedFiles: audit.unexpectedFiles } }], changedFiles: audit.changedFiles }); if (!isLoadedLargeEnemyBundle(loaded)) return operationResult({ command, status: "failed", input, errors: loaded.errors }); return operationResult({ command, status: "passed", input: { file: loaded.relativePath }, data: { profile: loaded.profile, resolvedDefinitionPaths: loaded.resolvedDefinitionPaths, targetableBodyPartIds: loaded.profile.bodyParts.filter((part) => part.targetable).map((part) => part.id) } }); }
