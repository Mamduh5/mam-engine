import { auditChangedFiles, captureWorkspaceState } from "../../infrastructure/files/changedFileAudit";
import { ErrorCodes } from "../../shared/errorCodes";
import { operationResult, type OperationResult } from "../../shared/operationResult";
import { isLoadedDamageReaction, loadValidDamageReaction } from "./damageReactionOperationSupport";

export async function inspectDamageReaction(workspaceRoot: string, inputFile: string): Promise<OperationResult> { const command = "damage-reaction.inspect"; const input = { file: inputFile }; const before = await captureWorkspaceState(workspaceRoot); const loaded = await loadValidDamageReaction(workspaceRoot, inputFile); const audit = auditChangedFiles(before, await captureWorkspaceState(workspaceRoot), []); if (!audit.ok) return operationResult({ command, status: "failed", input, errors: [{ code: ErrorCodes.DamageReactionWriteBlocked, message: "Read-only damage reaction inspection changed unexpected files", details: { unexpectedFiles: audit.unexpectedFiles } }], changedFiles: audit.changedFiles }); if (!isLoadedDamageReaction(loaded)) return operationResult({ command, status: "failed", input, errors: loaded.errors }); return operationResult({ command, status: "passed", input: { file: loaded.relativePath }, data: { profile: loaded.profile, derivedMetrics: { longestRecoveryDurationSeconds: Math.max(loaded.profile.hitReactionDurationSeconds, loaded.profile.staggerDurationSeconds) } } }); }
