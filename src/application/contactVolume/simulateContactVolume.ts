import { simulateContact } from "../../domain/contactVolume/contactVolumeSimulation";
import { auditChangedFiles, captureWorkspaceState } from "../../infrastructure/files/changedFileAudit";
import { ErrorCodes } from "../../shared/errorCodes";
import { operationResult, type OperationResult } from "../../shared/operationResult";
import { isLoadedContactVolume, loadValidContactVolume } from "./contactVolumeOperationSupport";

export async function simulateContactVolumeFiles(workspaceRoot: string, hitboxFile: string, hurtboxFile: string, fixedDelta?: number): Promise<OperationResult> { const command = "contact-volume.simulate-contact"; const input = { hitboxFile, hurtboxFile, ...(fixedDelta === undefined ? {} : { fixedDelta }) }; const before = await captureWorkspaceState(workspaceRoot); const hitbox = await loadValidContactVolume(workspaceRoot, hitboxFile); if (!isLoadedContactVolume(hitbox)) return operationResult({ command, status: "failed", input, errors: hitbox.errors }); const hurtbox = await loadValidContactVolume(workspaceRoot, hurtboxFile); if (!isLoadedContactVolume(hurtbox)) return operationResult({ command, status: "failed", input: { ...input, hitboxFile: hitbox.relativePath }, errors: hurtbox.errors }); if (hitbox.profile.role !== "hitbox") return roleFailure(command, { ...input, hitboxFile: hitbox.relativePath, hurtboxFile: hurtbox.relativePath }, hitbox.relativePath, "hitbox", hitbox.profile.role); if (hurtbox.profile.role !== "hurtbox") return roleFailure(command, { ...input, hitboxFile: hitbox.relativePath, hurtboxFile: hurtbox.relativePath }, hurtbox.relativePath, "hurtbox", hurtbox.profile.role); const simulation = simulateContact(hitbox.profile, hurtbox.profile, fixedDelta); const audit = auditChangedFiles(before, await captureWorkspaceState(workspaceRoot), []); if (!audit.ok) return operationResult({ command, status: "failed", input, errors: [{ code: ErrorCodes.ContactVolumeWriteBlocked, message: "Read-only contact simulation changed unexpected files", details: { unexpectedFiles: audit.unexpectedFiles } }], changedFiles: audit.changedFiles }); return operationResult({ command, status: "passed", input: { ...input, hitboxFile: hitbox.relativePath, hurtboxFile: hurtbox.relativePath }, data: simulation }); }
function roleFailure(command: string, input: Record<string, unknown>, path: string, expected: string, actual: string): OperationResult { return operationResult({ command, status: "failed", input, errors: [{ code: ErrorCodes.ContactVolumeRoleInvalid, path, message: `Contact volume must have role ${expected}`, expected, actual }] }); }
