import { auditChangedFiles, captureWorkspaceState } from "../../infrastructure/files/changedFileAudit";
import { ErrorCodes } from "../../shared/errorCodes";
import { operationResult, type OperationResult } from "../../shared/operationResult";
import { isLoadedOffensiveAction, loadValidOffensiveAction } from "./offensiveActionOperationSupport";

export async function inspectOffensiveAction(workspaceRoot: string, inputFile: string): Promise<OperationResult> { const command = "offensive-action.inspect"; const input = { file: inputFile }; const before = await captureWorkspaceState(workspaceRoot); const loaded = await loadValidOffensiveAction(workspaceRoot, inputFile); const audit = auditChangedFiles(before, await captureWorkspaceState(workspaceRoot), []); if (!audit.ok) return operationResult({ command, status: "failed", input, errors: [{ code: ErrorCodes.OffensiveActionWriteBlocked, message: "Read-only offensive action inspection changed unexpected files", details: { unexpectedFiles: audit.unexpectedFiles } }], changedFiles: audit.changedFiles }); if (!isLoadedOffensiveAction(loaded)) return operationResult({ command, status: "failed", input, errors: loaded.errors }); return operationResult({ command, status: "passed", input: { file: loaded.relativePath }, data: { profile: loaded.profile, derivedMetrics: { activeDurationSeconds: loaded.profile.activeEndSeconds - loaded.profile.activeStartSeconds, totalLifecycleSeconds: loaded.profile.durationSeconds + loaded.profile.cooldownSeconds } } }); }
